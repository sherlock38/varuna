"Ajout de séance" 
(lambda (a) (action-sous-menu-edition 'ajout 'veajout))

"Annulation de séance" 
(lambda (a) (action-sous-menu-edition 'annul 'veannul))

"Réservation de salles" 
(lambda (a) (action-sous-menu-edition 'resa 'veresa))

"Cours d'un professeur" 
(lambda (a) (action-sous-menu-edition 'profs 'veprof))

"Confirmation de cours" 
(lambda (a) (action-sous-menu-edition 'cours 'vecfcour)) 

